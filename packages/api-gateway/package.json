{
  "name": "@everybuddy/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API Gateway 抽象层：桌面端内部函数路由 + 未来 IM Bot 接入点",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@everybuddy/ipc-contract": "*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
